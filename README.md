Intelligent data cleaning and quality platform this platform will clean all the inconsistencies which are present inside the structured dataset.User after uploading the dataset then they can see the number of missing rows, noisy percentage, outlier percentage overall dataset score.then column importance will show the importance of the respected columns present inside the dataset.So,user can remove the unwanted columns,AI RECOMMENDATION this recommends the user what are the inconsistencies are present inside the dataset and what all we have to perform to remove all those inconsistencies and by this user can also apply the respected algorithms and clean the dataset and finally it will give the before and after scoring of the dataset and user can also download the complete cleaned dataset and can use it for developing ML , STATISTICAL, AI models and many more so that the trained model will be consistent and efficient 
